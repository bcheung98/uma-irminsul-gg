export interface HorseGirl {
    name: {
        en: string;
        jp: string;
    };
    description: string;
    birthday: string;
    height: number;
    sizes: [number, number, number];
    voiceActors: {
        jp: string;
    };
}
